version: '3.8'

services:
  reviewinsight:
    build: .
    container_name: reviewinsight-core
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./src:/app/src
      - ./cli:/app/cli
      - ./config:/app/config
    command: tail -f /dev/null
